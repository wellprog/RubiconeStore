<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Редактирование Категории"
             x:Class="RubiconeStore.MyViews.EditCategory">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Наименование" Margin="0, 40, 0, 0" HorizontalTextAlignment="Center" />
            <Entry Text="{Binding Name}" />

            <Label Text="Описание" Margin="0, 40, 0, 0" HorizontalTextAlignment="Center" />
            <Entry Text="{Binding Description}" />

            <Button Text="Сохранить" Command="{Binding SaveCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>